type DashboardDTO = {
  totalPersons: number;
  totalCategories: number;
  totalTransactions: number;
  totalIncome: number;
  totalExpense: number;
  balance: number;
};

export type { DashboardDTO };
